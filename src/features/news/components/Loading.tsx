import { LoadingContainer, LoadingTitle } from "../styled"

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingTitle>Cargando...</LoadingTitle>
    </LoadingContainer>
  )
}

export default Loading